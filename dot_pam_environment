EDITOR DEFAULT=nvim
TERMINAL DEFAULT=kitty
XKB_DEFAULT_OPTIONS DEFAULT=compose:caps
QT_QPA_PATFORMTHEME DEFAULT=gtk2
PATH DEFAULT=${HOME}/bin:${HOME}/.asdf/shims:${PATH}
SSH_AUTH_SOCK DEFAULT="${XDG_RUNTIME_DIR}/ssh-agent.socket"

